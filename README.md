# MeshPilot stdio MCP Fixture

Safe test repository for MeshPilot "从 GitHub 导入 stdio MCP".

Use this repository to validate the happy path:

- Root contains `mcp.json`
- `server_type` is `stdio`
- Command is `node server.js`
- Version is `1.0.0`
- Env input can be tested with `FIXTURE_TOKEN=fake-value`

This fixture does not contain real credentials or external calls.

